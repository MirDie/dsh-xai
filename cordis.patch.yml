# Optional SuperGrok / X Premium route over the base composition.
# Do not replace agent-default-model: a saved DeepSeek (or other) default
# should keep working after this bundle is installed. Users pick
# xai-oauth / <id> in the composer after signing in.

- insert:
    - id: llm-xai-oauth
      name: dsh-xai
